---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: everse-worker
  namespace: everse
  labels:
    app.kubernetes.io/name: everse-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: everse-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: everse-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: everse-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: everse-platform
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: "9090"
    spec:
      serviceAccountName: everse-worker
      nodeSelector:
        workload-type: batch
      tolerations:
        - key: workload-type
          operator: Equal
          value: batch
          effect: NoSchedule
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: ghcr.io/your-org/everse-worker:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 9090
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.observability:4317
            - name: OTEL_SERVICE_NAME
              value: everse-worker
            - name: EVAL_QUEUE_URL
              valueFrom:
                secretKeyRef:
                  name: everse-runtime
                  key: eval-queue-url
            - name: ARTIFACT_BUCKET
              valueFrom:
                secretKeyRef:
                  name: everse-runtime
                  key: artifact-bucket
            - name: MAX_CONCURRENT_EVALS
              value: "4"
          resources:
            requests:
              cpu: "2"
              memory: 4Gi
            limits:
              cpu: "8"
              memory: 16Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: everse-worker
  namespace: everse
  labels:
    app.kubernetes.io/name: everse-worker
spec:
  selector:
    app.kubernetes.io/name: everse-worker
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: everse-worker-sqs
  namespace: everse
spec:
  scaleTargetRef:
    name: everse-worker
  pollingInterval: 15
  cooldownPeriod: 300
  minReplicaCount: 2
  maxReplicaCount: 50
  fallback:
    failureThreshold: 3
    replicas: 6
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleUp:
          stabilizationWindowSeconds: 0
          policies:
            - type: Percent
              value: 200
              periodSeconds: 60
            - type: Pods
              value: 10
              periodSeconds: 60
          selectPolicy: Max
        scaleDown:
          stabilizationWindowSeconds: 600
          policies:
            - type: Percent
              value: 25
              periodSeconds: 120
  triggers:
    - type: aws-sqs-queue
      metadata:
        queueURLFromEnv: EVAL_QUEUE_URL
        queueLength: "10"
        awsRegion: us-east-1
        identityOwner: operator
    - type: prometheus
      metadata:
        serverAddress: http://prometheus-operated.observability.svc:9090
        threshold: "300"
        query: |
          max(aws_sqs_approximate_age_of_oldest_message_seconds{queue_name="everse-eval"})
